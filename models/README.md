Drop a custom-trained YOLO weights file here named `best.pt` to enable
the YOLO detector path (requires `ultralytics` + `torch` installed —
see requirements.txt). If this file isn't present, or ultralytics/torch
aren't installed, the app automatically uses the OpenCV bright-beacon
detector instead — the Live AI Camera panel works either way.
